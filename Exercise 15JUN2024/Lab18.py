# Explain the difference between the = operator and == operator in python:

# == operator is called as equality operator. It return boolean True if both are equal.

# = opeator is assignment operator. It assign values to the variables.
